OpenCMISS Dependencies README

Start the cmake script with -DOCM_DEVELOPER_MODE=ON
to checkout the git repositories associated with all the submodules.
otherwise, only the source files will be downloaded. 

todo: write docs